<p>
  <%= comment.body %>
  <% if comment.user %>
  <small>
  <%= comment.user.username %> commented <%= time_ago_in_words(comment.created_at) %> ago
     
     <% if policy(comment).destroy? %>
    
     <% end %>
  </small>
  <% end %>
</p>